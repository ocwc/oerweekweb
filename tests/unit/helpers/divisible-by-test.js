import { divisibleBy } from '../../../helpers/divisible-by';
import { module, test } from 'qunit';

module('Unit | Helper | divisible by');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = divisibleBy([42]);
  assert.ok(result);
});
